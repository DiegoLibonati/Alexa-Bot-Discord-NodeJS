const playMusicBot = require("./playMusicBot");
const skipMusicBot = require("./skipMusicBot");
const stopMusicBot = require("./stopMusicBot");
const pauseMusicBot = require("./pauseMusicBot");
const resumeMusicBot = require("./resumeMusicBot");

module.exports = {
  playMusicBot: playMusicBot,
  skipMusicBot: skipMusicBot,
  stopMusicBot: stopMusicBot,
  pauseMusicBot: pauseMusicBot,
  resumeMusicBot: resumeMusicBot,
};
