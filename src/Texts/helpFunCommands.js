const helpFunCommands = () => {
  commands =
    "PREFIX: diego \n\ndiego **say** <text> - This command allows you to type a text to the bot and have the bot speak. [ONLY WORKS IN ALEXA MODE].\n";

  return commands;
};

module.exports = helpFunCommands;
